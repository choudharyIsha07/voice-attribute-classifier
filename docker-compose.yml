version: '3.8'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - APP_NAME=Voice Attribute Classifier
      - ENVIRONMENT=production
      - LOG_LEVEL=INFO
      - MAX_UPLOAD_SIZE=10485760
